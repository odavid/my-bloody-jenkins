version: '2'
services:
  consul:
    image: consul
    ports:
      - "8400:8400"
      - "8500:8500"
      - "8600:8600"
      - "8600:8600/udp"
    volumes:
      - ${TESTS_DIR}:${TESTS_CONTAINER_TESTS_DIR}


networks: 
  default:
    external:
      name: ${JENKINS_DOCKER_NETWORK_NAME}